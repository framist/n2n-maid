# N2N Maid 安装程序界面资源 🎨

恩兔已经为主人准备好了安装程序的自定义文案！现在需要一些可爱的图片来配合～

## 需要的图片文件

### NSIS 安装包（.exe 格式）

1. **nsis-header.bmp** (150 x 57 像素)
   - 安装向导顶部横幅
   - 建议：粉色渐变背景 + 恩兔酱的小头像

2. **nsis-sidebar.bmp** (164 x 314 像素)
   - 安装向导左侧装饰图
   - 建议：恩兔酱全身立绘 + 粉白配色

### WiX MSI 安装包（.msi 格式）

3. **dialog.bmp** (493 x 312 像素)
   - 欢迎页面和完成页面的背景图
   - 建议：恩兔酱拿着鸡毛掸子的插图 + "恩兔酱"标题

4. **banner.bmp** (493 x 58 像素)  
   - 其他对话框顶部横幅
   - 建议：粉色渐变 + N2N Maid logo

## 图片规格要求

- **格式**：BMP（Windows 位图）
- **色彩**：24位真彩色（推荐）或 8位索引色
- **尺寸**：必须严格符合上述像素规格
- **风格**：粉色主色调（#ffd1dc）+ 白色，符合可爱女仆主题

## 临时方案

如果主人暂时没有准备图片，恩兔可以先用纯色背景：
- 删除 `tauri.conf.json` 中的 `dialogImagePath`、`bannerPath`、`headerImage`、`sidebarImage` 配置
- 或者使用简单的纯色 BMP 占位符

## 创建示例图片（可选）

如果主人想快速测试，可以用 Python 或在线工具创建简单的占位符：

```python
from PIL import Image

# NSIS Header
header = Image.new('RGB', (150, 57), '#ffd1dc')
header.save('icons/nsis-header.bmp')

# NSIS Sidebar  
sidebar = Image.new('RGB', (164, 314), '#ffd1dc')
sidebar.save('icons/nsis-sidebar.bmp')

# WiX Dialog
dialog = Image.new('RGB', (493, 312), '#ffffff')
dialog.save('icons/dialog.bmp')

# WiX Banner
banner = Image.new('RGB', (493, 58), '#ffd1dc')
banner.save('icons/banner.bmp')
```

---

**提示**：图片准备好后，放到 `src-tauri/icons/` 目录下，然后重新运行 `npm run tauri build` 即可～
